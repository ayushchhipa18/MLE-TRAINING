apiVersion: v1
kind : Service
metadata:
  name: diabetes-service
spec:
  type: ClusterIP
  selector:
    app: diabetes
  ports:
    - name: fastapi
      port: 80
      targetPort: 8000
    - name: streamlit
      port: 8501
      targetPort: 8501
      