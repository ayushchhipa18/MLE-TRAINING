apiVersion: eksctl.io/v1alpha5
kind: ClusterConfig

metadata:
  name: diabetes-eks
  region: ap-south-1

iam:
  withOIDC: true

fargateProfiles:
  - name: fp-default
    selectors:
      - namespace: default
      - namespace: kube-system